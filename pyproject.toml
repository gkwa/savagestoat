[project]
name = "savagestoat"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
savagestoat = "savagestoat.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
